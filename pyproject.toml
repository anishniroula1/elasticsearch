[project]
name = "ner-similarities-elasticsearch-demo"
version = "0.1.0"
description = "Local FastAPI and Elasticsearch demo for NER entity similarities"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "elasticsearch>=9,<10",
  "fastapi>=0.115,<1",
  "pandas>=3,<4",
  "pydantic>=2.8,<3",
  "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false
